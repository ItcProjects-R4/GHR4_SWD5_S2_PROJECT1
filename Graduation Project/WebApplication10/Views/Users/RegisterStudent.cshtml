@model PathwayPlatform.Models.User
@{ ViewData["Title"] = "Register as Student"; }

<div class="auth-wrap">
    <div class="auth-card card">
        <div class="flex-between mb-4">
            <a asp-controller="Users" asp-action="Register" class="text-sm text-muted">← Back</a>
            <span class="badge">Student account</span>
        </div>
        <h1 style="font-size:24px;">Create your account</h1>
        <p class="text-muted text-sm mt-2">Explore, learn, and grow at your own pace.</p>

        @if (ViewBag.Error != null)
        {
            <div class="alert-error mt-4">@ViewBag.Error</div>
        }

        <form asp-controller="Users" asp-action="RegisterStudent" method="post" class="mt-5">
            @Html.AntiForgeryToken()
            <div class="field">
                <label asp-for="Name">Full name</label>
                <input asp-for="Name" placeholder="Jane Doe" required />
                <span asp-validation-for="Name" class="field-error"></span>
            </div>
            <div class="field">
                <label asp-for="Email">Email</label>
                <input asp-for="Email" type="email" placeholder="you@example.com" required />
                <span asp-validation-for="Email" class="field-error"></span>
            </div>
            <div class="field">
                <label asp-for="Password">Password</label>
                <input asp-for="Password" type="password" placeholder="At least 6 characters" required minlength="6" />
                <span asp-validation-for="Password" class="field-error"></span>
            </div>
            <button class="btn btn-primary mt-5" style="width:100%;" type="submit">Create student account</button>
        </form>

        <p class="text-sm text-muted mt-4" style="text-align:center;">
            Already have an account?
            <a asp-controller="Users" asp-action="Login" style="color:var(--primary);font-weight:500;">Sign in</a>
        </p>
    </div>
</div>
